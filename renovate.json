{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": [
        "^charts/.+/values\\.yaml$"
      ],
      "matchStrings": [
        "registry:\\s*(?<registryUrl>[^\\s]+)\\s+repository:\\s*(?<depName>[^\\s]+)\\s+tag:\\s*[\"']?(?<currentValue>[^\\s\"']+)[\"']?"
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "semver"
    }
  ],
  "docker": {
    "pinDigests": false
  },
  "helm-values": {
    "fileMatch": ["^charts/.+/Chart\\.yaml$"]
  },
  "packageRules": [
    {
      "description": "Track version tags for container images (grouped & scheduled)",
      "matchDatasources": ["docker"],
      "matchPackagePrefixes": ["chillincool/"],
      "versioning": "semver",
      "groupName": "container images",
      "schedule": ["before 06:00 every day"],
      "automerge": false,
      "commitMessageTopic": "{{depName}}",
      "separateMinorPatch": false,
      "prConcurrentLimit": 2
    },
    {
      "description": "Separate critical security updates",
      "matchUpdateTypes": ["major"],
      "groupName": "major container updates",
      "schedule": ["at any time"],
      "automerge": false
    }
  ],
  "prConcurrentLimit": 2,
  "prCreation": "not-pending",
  "labels": ["dependencies", "renovate"],
  "commitMessagePrefix": "chore(deps):",
  "commitMessageAction": "update",
  "commitMessageExtra": "to {{newValue}}",
  "semanticCommits": "enabled"
}
